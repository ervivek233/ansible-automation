---
- name: Test playbook from GitHub
  hosts: target
  become: yes

  tasks:
    - name: Check hostname
      command: hostname
      register: result

    - name: Display hostname
      debug:
        var: result.stdout
